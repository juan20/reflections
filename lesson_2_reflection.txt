What happens when you initialize a repository? Why do you need to do it?

	a folder its created and i you need to do it because is here were you 
	git saved the metadata that contains the status of the repository.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

	The staging area is diffrent from the working diretory because heres is were
	the file that are going to be commit are storaged and the repository have all
	of the files in ones. it's value because offers you the chance to control the
	files that are going to be commited and keep a logical changes per commit.

How can you use the staging area to make sure you have one commit per logical
change?
	
	searching the difference between the last commit and the staging area and
	comparing the 2 files.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

		
	To create experimental projects of the version you already had.	
	The branches organize the follow of coding.

How do the diagrams help you visualize the branch structure?

	it help you, because you can visualize how the code is going.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

	a combiantion of the 2 code adding the code from the 2 branches and deleting 
	the ones that were removed by a person

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?

	that can be found a conflict when a merge 2 files into a single one.
